<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    tools:context=".P25_SeleccionElemento">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!--Nombre principal del item (comun a todos)-->
        <TextView
            android:id="@+id/detalle_txt_titulo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text"
            android:textSize="28sp"
            android:textStyle="bold"
            tools:text="Titulo del Item" />

        <!--Descripición del item (común a todos: autor libro/numero revista/marca articulo-->
        <TextView
            android:id="@+id/detalle_txt_descripcion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/text"
            android:textSize="18dp"
            tools:text="Autor/Número/Marca" />

        <!--Separador-->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="24dp"
            android:background="@color/form_text" />

       <!--Recycler View con las caracteristicas especificas de cada item-->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/detalle_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:nestedScrollingEnabled="false"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:itemCount="5"
            tools:listItem="@layout/item_detalle_fila" />
    </LinearLayout>
</ScrollView>